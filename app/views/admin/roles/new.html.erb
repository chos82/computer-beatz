<h1>New role</h1>

<% form_for( @role, :url => {:controller => 'admin/roles', :action => 'create'} ) do |f| %>
  <%= f.error_messages %>
  <p>
  	<%= f.label :rolename %>
  	<%= f.text_field :rolename %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_roles_path %>
