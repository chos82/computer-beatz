<div class="margin-left">
<p>
	<h3><%= h @artist.name %></h3>
</p>
<% form_for( @artist, :url => {:action => 'check_added_track', :id => @artist, :method => :post} )do |f| %>
  <p>
    <%= f.label :track %><br />
    <%= text_field_with_auto_complete :track, 
									  :name,
									  {:autocomplete => 'off'},
									  {:method => :get, :skip_style => true, :url => {:controller => 'tracks', :action => 'auto_complete_for_track_name'} } %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>
</div>